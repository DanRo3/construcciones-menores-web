import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Productos | Construcciones Menores",
    description: "Esta es la página de productos de la empresa Construcciones Menores",
    // other metadata
};

const ProductPage = () => {
  return (
    <div>
      
    </div>
  )
}

export default ProductPage;
